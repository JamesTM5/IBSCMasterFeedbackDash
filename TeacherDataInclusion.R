# for a given student file, find a matching teacher one.
# if there is one:
#   Turn the edges frames into variables
#   If they contain no rows, break.
#   convert text to numeric
#   For each student, Take the mean
#   Add the mean to the individual question answers
#   Add the above to the class nodes 

includeTeacherData <- function() {
  
}